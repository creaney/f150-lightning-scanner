# Scanner Tests

Run all tests:

```
python3 -m pytest -q
```

Expected output includes passing tests and a count of `xfail` items.
The `xfail` cases are confirmed bugs that Phase 1 is expected to fix.
